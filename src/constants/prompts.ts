
import { PROMPTS as allPrompts } from './be-prompts';

// This file re-exports to maintain the original structure for dependent files.
// The core prompt logic now resides in 'be-prompts.ts'.

export const PROMPTS = allPrompts;
